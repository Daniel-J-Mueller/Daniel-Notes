This works well with the new-token-mechanism concept.

Reduce an entire output into one speculative title, then allow another full edit to reference it in place.

At the end of the full generation, parse them together.